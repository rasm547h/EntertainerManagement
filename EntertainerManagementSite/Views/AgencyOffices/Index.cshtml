@model IEnumerable<EntertainerManagementSite.Models.AgencyOffice>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Address.Address_address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Agency.Agency_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OfficeContactInformation.ContactInfo_phoneNo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Address.Address_address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Agency.Agency_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OfficeContactInformation.ContactInfo_phoneNo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FK_addressID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FK_addressID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FK_addressID })
        </td>
    </tr>
}

</table>
